# Da Vinci Visual QA v2

`REFERENCE → PLAN → BUILD → CAPTURE → COMPARE → PATCH → CAPTURE → PASS`

Each visual task must define viewport(s), accessibility criteria, responsive breakpoints, motion/reduced-motion behavior, and acceptance screenshots.

A visual QA report must separate defects from subjective polish suggestions.
