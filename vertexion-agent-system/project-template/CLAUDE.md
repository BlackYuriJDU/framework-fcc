# Projeto gerenciado pelo Vertexion Agent System

Antes de editar:

- leia `PROJECT_PROFILE.md` e `docs/requirements/current.md`;
- respeite o escopo;
- não leia `.env`;
- trate migrations, pagamentos, autenticação e produção como alto risco;
- use `/revisar`, `/validar` e `/preview` nos momentos adequados;
- registre decisões e limitações.
