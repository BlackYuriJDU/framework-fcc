# FCC Task State

`tasks/` guarda estado operacional e artefatos de tarefas. Não misturar com memória durável (`memory/`), conhecimento externo (`knowledge/`) ou eventos de runtime (`state/control-center/`).
