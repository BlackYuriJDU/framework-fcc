# Worktree Policy v8

Parallel implementation is allowed only for independent scopes. Each branch/worktree must map to one Task Contract. Integration occurs only after evaluator review. Never share mutable files across active worktrees unless the contract declares the dependency.
